const { intentClassifier } = require('./questionExemplexemple');

// Maintenant, vous pouvez utiliser intentClassifier dans ce fichier
console.log(intentClassifier.classify("Je veux boire un barcadi"));